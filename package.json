{
  "name": "mel-storyboard",
  "private": true,
  "type": "module",
  "scripts": {
    "test": "node --test tests/*.test.mjs",
    "check": "node --check scripts/main.js && node --check scripts/domain/*.js && node --check scripts/ui/*.js"
  }
}
